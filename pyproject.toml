[tool.ruff]
line-length = 88
# Markdown code blocks (BOOT_FRAMES.md) are illustrative; ruff >= 0.16 would
# otherwise try to format them.
extend-exclude = ["*.md"]
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.format]
quote-style = "double"